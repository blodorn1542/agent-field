# Per-tenant keys are pasted by each company and stored via the credentials port;
# these are only the fallback/bootstrap values for a single-tenant dev run.
# Your field system's GraphQL endpoint. No default ships with this package.
POM_ENDPOINT=
POM_API_KEY_READONLY=pom_live_...
